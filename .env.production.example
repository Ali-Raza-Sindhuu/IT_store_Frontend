# Public values only. Vite embeds VITE_* variables into the browser bundle.
# Copy these values to the hosting provider's production environment settings.
VITE_API_BASE_URL=https://api.yourdomain.com/api
